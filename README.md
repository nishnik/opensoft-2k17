# opensoft-2k17

DApp : Blockchain based app

Open `testrpc` in one terminal

Open `ipfs daemon` in other

Then run `embark run` in the ethpic directory

NOTE: This was our initial attempt at backend for the **silver** winning software. This is not our final code. The actual code would be uploaded to Azad Hall's repository.
